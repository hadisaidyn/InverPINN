# GitHub-polish verification

Date: 2026-09-20. Starting branch: `scientific-revision`; clean at
`5cc8e02d83fcf5180ae17426f9b0c57153fd5d5f`.
This is a presentation audit, not a new scientific experiment.

## Verified scope

- All 265 previously tracked source/config/script/test/paper files remain
  byte-identical to the starting commit. New files add documentation checks only.
- Frozen model configurations, manifests, data, raw results, figure data,
  canonical paper outputs and scientific interpretations are unchanged.
- Two historical report files received only local-archive link clarifications;
  their scientific text/numbers were not revised.
- Seven links targeted ignored outputs unavailable on GitHub. They are now
  explicitly local-only references with links to the archive-access guide.
- Converted 113 machine-specific paths in the public historical file inventory
  to repository-relative paths. Two paths in the sealed numerical configuration
  remain as original provenance. Path-detector source/tests contain synthetic
  patterns, not live user filesystem links.

## Commands and environment

The [README setup](../README.md#quick-start) was verified with a fresh temporary
Python 3.13.3 virtual environment instead of modifying the existing scientific
environment. The unchanged pinned report dependencies installed, the editable
package installed with `--no-deps`, and `import inverpinn` succeeded. PyTorch was
absent from this reporting environment. Initial sandbox DNS restrictions blocked
installation; an approved network-enabled retry succeeded without changing pins.

- `scripts/build_paper.py --verify-only`: passed, including all 41 evidence hashes.
- `scripts/check_docs.py`: all public relative links, images and anchors passed;
  external URLs are not HTTP availability checks.
- `python -m pytest tests/test_paper_reporting.py tests/test_repository_docs.py -q`:
  **42 passed, 0 failed, 0 skipped, 0 warnings** in the final run.
- `scripts/build_paper.py --output-dir paper/reproduced_github_check`: passed;
  4.03 seconds, about 1.6 MB output. Its summary exactly equals canonical JSON.
- `scripts/build_paper.py --output-dir paper/reproduced_ci`: passed locally.
- A new reference-link parser test initially failed because one label was
  counted twice. The parser was corrected; no test expectation was weakened.

No training, checkpoint inference, numerical simulation or full scientific
suite was run. The archived 451-test scientific result is historical only.
GitHub-hosted execution has not occurred: no remote is configured. The local
workflow checks passed on macOS ARM64; hosted Ubuntu execution is not asserted.

## Strict archive-check caveat

The extra `scripts/package_paper_evidence.py --verify-preservation` check
reported changes to three **ignored, generated packaging metadata** files:

- `src/inverpinn.egg-info/PKG-INFO`
- `src/inverpinn.egg-info/SOURCES.txt`
- `src/inverpinn.egg-info/requires.txt`

These were regenerated by the explicitly requested editable-install test;
they are not scientific source, configurations, observations or results.
The inventory comparison found **13,396/13,399 files byte-identical**, precisely
those three exceptions, and no added/deleted inventory entries. All scientific
files remain identical. The strict checker still reports this difference;
neither its original inventory nor its criteria were altered. Do not report
that the unqualified 13,399-file check passed after package installation.

## Publication hygiene

Credential-format and contextual scans found no probable secrets or accidental
tracked caches. The largest tracked binary is an intentional 1.22 MB field-preview
NPZ; no LFS migration or archive removal is warranted for the current tree.
Existing figures are referenced, not duplicated.

Citation authorship uses the Git name **Hadis** and existing package version
**0.0.0**, without invented identity or publication metadata. No license choice,
GitHub remote, release tag, hosted CI status, DOI or manuscript PDF was invented.
No push or publication was performed. Official Actions v6 revisions were
verified and pinned; CI only reads evidence and builds reports.

[Documentation index](README.md) · [Reproduction guide](reproducibility.md).
